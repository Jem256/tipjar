export interface UserData {
    email: string;
    name: string;
    slug: string;
    balance: number;
}

export interface loginData {
    email: string;
    password: string;
}

export interface registerData {
    name: string;
    email: string;
    password: string;
}
